# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['deal_compass.py'],
             pathex=['D:\\projects\\S_Git_proj\\spider\\Other\\spider_all\\Demo\\数据处理（筛选）\\compass\\企查查模板数据'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='deal_compass',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True , icon='a.ico')
